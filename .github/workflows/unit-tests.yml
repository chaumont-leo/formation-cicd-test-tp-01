name: Unit Tests

on: 
  push:
    branches: 
      - lbouhier

jobs:
    test:
        runs-on: ubuntu-latest
        
        steps:
            - name: Checkout repository
              uses: actions/checkout@v5

            - name: Set up JDK 17 for x64
              uses: actions/setup-java@v4
              with:
                java-version: '17'
                distribution: 'corretto'
                architecture: x64
                cache: 'maven'

            - name: Run the Maven tests (PasswordPolicyTest)
              run: mvn clean test -Dtest=com.devops.cicd.PasswordPolicyTest
            
            - name: Run the Maven tests (PricingServiceTest)
              run: mvn clean test -Dtest=com.devops.cicd.PricingServiceTest